#!/usr/bin/env bash

# content_shell
echo $ASDF_INSTALL_PATH
CONTENT_SHELL_DIR=$(ls $ASDF_INSTALL_PATH | grep "drt-")
DARTIUM_DIR=$(ls $ASDF_INSTALL_PATH | grep "dartium-")

echo -n "dart-sdk/bin $CONTENT_SHELL $DARTIUM_DIR"
